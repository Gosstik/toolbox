#add_subdirectory(binomial_heap)

# Uncomment next line to make coverage of all sources tested in current dir.
#make_directory_coverage(${CMAKE_CURRENT_SOURCE_DIR})

# --------------------------------------------------------------------
